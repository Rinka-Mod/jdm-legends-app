node_modules/
dist/
*.log

# SQLite (dữ liệu sinh ra khi chạy server lần đầu)
server/data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Biến môi trường — chặn MỌI biến thể (.env.production, .env.development…),
# vì chỉ cần lọt một file là lộ khoá bí mật. Chỉ giữ lại file mẫu (không có bí mật).
.env
.env.*
!.env.example

# Hệ điều hành / trình soạn thảo
.DS_Store
Thumbs.db
.vscode/
.idea/
